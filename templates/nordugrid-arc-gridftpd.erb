/var/log/arc/gridftpd.log {
  missingok
  compress
  delaycompress
  daily
  rotate 90
  create
  postrotate
	kill -HUP `cat <%= @run_directory %>/gridftpd.pid 2> /dev/null` 2> /dev/null || true
  endscript
}
